// import * as aT from '../actions/types';

const INITIAL_STATE = { abort: false, navigateTo: '' };

export default (state = INITIAL_STATE, action) => {
	// switch (action.type) {
	// 	case aT.ABORT_REQUEST:
	// 		return { ...INITIAL_STATE, abort: true };
	// 	case aT.PAGE_NAVIGATION:
	// 		return { ...state, navigateTo: action.payload };
	// 	case aT.MAKE_NEW_REQUEST:
	// 		return { ...INITIAL_STATE }
	// }
	return 0;
};
